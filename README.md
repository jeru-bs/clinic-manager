Production application: `docs/`
Legacy, inactive implementation: `src/`

# ניהול קליניקה

מערכת Web בעברית ובכיוון RTL לניהול קליניקה: מטופלים, הורים ואנשי מקצוע, יומן, מטרות טיפול, שאלונים, דוחות טיפוליים, תיעוד מפגשים, משימות, תשלומים וקבצים.

הגרסה הפעילה מתפרסמת דרך GitHub Pages מתוך תיקיית `docs`.

## מה קיים עכשיו

- ממשק עבודה בדפדפן עם סיידבר ימני.
- חיבור Google OAuth מתוך הדפדפן.
- בדיקת חשבון Google מורשה לפני טעינת נתונים.
- שמירת מטופלים, מפגשים, תשלומים, משימות וקבצים ב-Google Sheets וב-Google Drive.
- יצירת תיקייה נפרדת לכל מטופל בתוך תיקיית האחסון הראשית.
- העלאת קבצים מתחדשת לכרטיס מטופל, עם התקדמות, ניסיון חוזר וביטול.
- מחיקת קבצים מכרטיס המטופל ומהדרייב לאחר אישור.
- תיעוד מפגש במסמך Google Docs ושמירת קישור בכרטיס המטופל.
- סנכרון מפגש שמור ל-Google Calendar כאירוע פרטי וכללי, ללא שם מטופל, תיעוד או מיקום.
- תור סנכרון מקומי לפעולות יומן ומסמכים שנכשלו, עם ניסיונות חוזרים וכפתור ניסיון ידני.
- חיווי קבוע של מצב השמירה וזמן הרענון האחרון.
- ניהול מלא של הורים, בני משפחה ואנשי מקצוע מתוך כרטיס המטופל.
- מטרות טיפול מובנות עם מצב, אחוז התקדמות, תאריך יעד והיסטוריית עדכונים מתוך מפגשים.
- שתי תבניות שאלון ניתנות לעריכה להורים ולאנשי מקצוע; לכל שליחה נוצר Google Form ייחודי ללא שם המטופל בכותרת.
- שליחת קישור שאלון דרך הודעת WhatsApp או מייל מוכנים, קליטת תשובות ידנית וסגירת הטופס לאחר הקליטה.
- הפקת דוח אבחון, דוח התקדמות ודוח סיכום טיפול כ-Google Doc וכ-PDF, לאחר עריכת הטיוטה וללא הכללה אוטומטית של הערות רגישות.
- תצוגת יומן חודשית, כולל מפגשים קבועים שמחושבים מתוך הגדרת המטופל.
- שמירת מפגש קבוע מתוך היומן כמפגש רגיל עם סנכרון ותיעוד.
- חריגי יומן למפגשים קבועים: ביטול חד-פעמי, חופשות, חגים וימים חסומים.
- לוח מועדי ישראל אוטומטי מאת Hebcal; ימי Yom Tov חוסמים הקרנה של מפגש קבוע.
- ניהול משימות ותשלומים.
- תזכורות בסיסיות מתוך משימות, עם תאריך תזכורת ותצוגה בדשבורד.
- יומן Audit מרכזי ו-Undo לפעולות שניתנות לביטול בטוח; פעולות חיצוניות בלתי-הפיכות מתועדות ללא כפתור ביטול מטעה.
- יצוא טבלאות, גיבוי מלא לקובץ או לדרייב, ושחזור מאומת עם גיבוי מקדים ו-rollback אוטומטי במקרה כשל.
- בדיקת תקינות מבנה Google Sheets ותיקון גיליונות/כותרות חסרים.

## אחסון Google

תיקיית האחסון הראשית:

```text
https://drive.google.com/drive/folders/16XWqhBGBZ053aRGPmuWTv0MOsM-d7JfV
```

מזהה התיקייה:

```text
16XWqhBGBZ053aRGPmuWTv0MOsM-d7JfV
```

הגדרות הפרויקט המפורסם נמצאות ב-`docs/config.js`.

## טבלאות מערכת

המערכת משתמשת בגיליונות הבאים:

- `patients`
- `sessions`
- `payments`
- `tasks`
- `files`
- `contacts`
- `goals`
- `goal_updates`
- `questionnaire_templates`
- `questionnaire_assignments`
- `questionnaire_responses`
- `clinical_reports`
- `schedule_exceptions`
- `audit_log`

מבנה העמודות מוגדר ב-`docs/app.js` בתחילת הקובץ ומתועד גם ב-`docs/DATA_MODEL.md`.

## מה עדיין לא סגור

- רשימת חשבונות Google מורשים היא חובה; רשימה ריקה חוסמת גישה. אסימון הגישה נשמר לסשן בלבד, והמערכת מנסה לשחזר אוטומטית חיבור שכבר אושר במכשיר.
- הרשאות Google Sheets ו-Google Drive הן גבול האבטחה הקשיח של גרסת GitHub Pages. להגנה מלאה באמצעות סשן שרת נדרש להעביר את המוצר הפעיל לאירוח פרטי עם backend.
- בכל התחברות מורשית המערכת בודקת את גיליון הנתונים ותיקיות הקליניקה ומסירה אוטומטית הרשאות שיתוף ציבוריות מסוג `Anyone` לפני טעינת הנתונים.
- מועדי ישראל נטענים משירות Hebcal ונשמרים במטמון מקומי. חופשות פרטיות וחסימות נוספות ממשיכות להיות מוזנות ידנית בהגדרות.
- תמלול AI להקלטות עדיין לא מחובר.
- חשבונית ירוקה עדיין לא מחוברת.
- בפעם הראשונה לאחר השדרוג צריך להפעיל בפרויקט Google Cloud את Google Forms API ולהתחבר מחדש כדי לאשר קריאת תשובות. אין צורך בשרת, Pub/Sub או שירות חיצוני בתשלום.

## הרצה מקומית

לבדיקת קוד סטטי:

```powershell
node --check docs\app.js
```

בדיקת שחרור מלאה:

```powershell
npm.cmd run release:check
```

הפקודה בודקת קוד סטטי, פרטיות היומן, תור סנכרון, העלאות מתחדשות, ניהול אנשי קשר ומטרות, מחזור שאלון, שלושת הדוחות, כללי אבטחת דפדפן, תהליכי Audit/Undo ותזכורות, תקלות Google, אימות גיבוי/rollback ו-lint.

אם עובדים על גרסת Next הישנה, יש להשתמש ב-`npm.cmd` ב-Windows:

```powershell
npm.cmd install
npm.cmd run dev
```

הקבצים המקומיים `work/` ו-`.env.local` אינם מיועדים לפרסום לגיטהאב.

## כללי עבודה

לפני שינוי לא טריוויאלי בפרויקט, מתחילים מ-`docs/WORKING_PRINCIPLES.md`: זה מסמך ה-SSOT, Root Cause First וחיסכון הטוקנים של העבודה.

הגרסה היציבה הנוכחית היא `1.1.0`. פירוט השחרור נמצא ב-`docs/RELEASE_NOTES.md`.
