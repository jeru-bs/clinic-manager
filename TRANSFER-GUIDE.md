# מעבר המערכת למחשב חדש

## מה להעתיק

יש להעתיק את כל התיקייה `Clinic-Manager-Handoff-2026-08-19` לכונן חיצוני או ישירות למחשב החדש. לאחר ההעתקה משווים את `CHECKSUMS.sha256` כדי לוודא שכל הקבצים עברו בשלמותם.

## התקנה במחשב החדש

1. מתקינים Git וגרסת Node.js עדכנית מסוג LTS.
2. מעתיקים את `project-source` למיקום העבודה הרצוי.
3. פותחים חלון PowerShell בתיקיית הפרויקט ומריצים `npm.cmd install`.
4. מריצים `npm.cmd run release:check` ומוודאים שכל הבדיקות עוברות.

## התחברות למערכת הפעילה

1. פותחים את כתובת GitHub Pages של המערכת.
2. מתחברים מחדש לחשבון Google המורשה. אסימוני ההתחברות מהמחשב הישן אינם מועברים.
3. אם Google מבקש הרשאה חדשה, מאשרים קריאת תשובות מ-Google Forms.
4. אם השאלונים אינם עובדים, נכנסים לקישור Google Forms API במסך ההגדרות ומפעילים את ה-API באותו פרויקט Google Cloud של מזהה ההתחברות.
5. פותחים מטופל, בודקים שהקבצים והמטרות מופיעים, ויוצרים שאלון בדיקה ודוח בדיקה.

## שחזור במקרה הצורך

קובצי הגיבוי נמצאים ב-`data-backups`. במסך ההגדרות של המערכת בוחרים שחזור מגיבוי, בוחרים את הגיבוי הסופי ומאשרים. המערכת שומרת אוטומטית גיבוי נוסף לפני החלפת נתונים.

קובצי המטופלים עצמם נשארים ב-Google Drive ואינם מועתקים מקומית. הגיבוי כולל את רשומות הנתונים ומזהי הקישורים אל הקבצים.

## בדיקת שלמות

מתוך PowerShell בתוך תיקיית ההעברה אפשר להריץ:

```powershell
Get-Content CHECKSUMS.sha256 | ForEach-Object {
  $hash, $relative = $_ -split '  ', 2
  $actual = (Get-FileHash -Algorithm SHA256 -LiteralPath $relative).Hash.ToLower()
  if ($actual -ne $hash) { Write-Error "קובץ לא תקין: $relative" }
}
```

אם לא התקבלה שגיאה, תכולת החבילה עברה בשלמותה.
